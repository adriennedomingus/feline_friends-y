<%= form_for([:admin, order], method: :patch) do |f| %>
  <input type="hidden" name="status" value="<%= status %>">
  <%= f.submit status_spelled, locals: {status: status }, class: "button btn" %>
<% end %>
